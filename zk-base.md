# Спецификация протокола PoSoc (v0.9)

## 1. Назначение, принципы, модель угроз

### 1.1 Назначение

Цель — децентрализованная сеть доверия (**PoSoc — Proof of Social**), позволяющая каждому участнику отличать реальных людей от ботов/виртуальных аккаунтов **в пределах наблюдаемого им компонента социального графа**. Гарантии — условные и вероятностные: они формулируются в терминах стоимости атаки, а не как абсолютные свойства.

### 1.2 Принципы

1. **Отсутствие инфраструктуры.** Нет корневых узлов, валидаторов, субстрата упорядочивания и журнала. Сеть — сами участники и их связи.
2. **Одноранговость.** Полномочий нет; каждый участник валидирует состояние сам.
3. **Субъективное локальное доверие.** Участник доверяет иерархиям, достижимым из его L0 (§6).
4. **Стоимость вместо альтруизма.** Протокол не предполагает, что участники не подтверждают ботов: он делает подтверждение ботов **стоимостным и рекуррентным**. Подкуп и обман — векторы стоимости в модели угроз (§1.3), а не нарушения допущений.
5. **Публичность внутри компонента.** Все записи публичны в пределах компонента связности; валидность — чистая функция данных.
6. **Эфемерность.** Все факты — автопродлеваемые TTL-объекты с мгновенным отзывом; однажды «купленного» факта не существует.
7. **Взаимность согласия.** Вход двусторонен (обе подписи / кворум), выход и отзыв — односторонни. Молчаливого согласия нет.
8. **Эмерджентность иерархии.** Рёбра и уровни — выводимое состояние, не события.
9. **Время подписи вместо порядка доставки.** Состояние — функция множества подписанных записей и текущего времени; порядок получения записей семантики не имеет.

### 1.3 Модель угроз

**Противник может:** создавать неограниченно ключей (Sybil); полностью контролировать связи своих узлов; создавать группы и декларации (permissionless); получать связи с людьми (встреча, обман, подкуп); похищать ключи; **задерживать или утаивать записи при ретрансляции**; смещать собственные часы.

**Противник не может (криптографически):** подделать подпись; создать/продлить объект согласия без требуемых подписей; включить участника в L0 без его подписи; разорвать чужую связь; аннулировать метку яда без подписи разорвавшей стороны; привязать группу к иерархии без живых внешних связей и кворума деклараций.

**Гарантии (в пределах допущений):**
- *Стоимость включения:* включение бот-компонента в человеческую иерархию требует $\approx t$ взаимных живых связей с людьми; стоимость линейна по порогу и **рекуррентна** (всё истекает за $\le T_{life}$).
- *Изоляция:* декларации без живых внешних связей не создают рёбер (дизъюнктный подсчёт, §5.4); транспортная изоляция дублирует структурную (§2.2).
- *Локализация угона:* окна сжимаются в $1+N$ раз; чужие метки угонщик не стирает; исключение — кворумом отзывов, мгновенно.

**Негарантии:** многоличность (§10.2); сговор выше порогов (§10.5); приватность (§11); мгновенная глобальная согласованность (сходимость — в пределе, §2.5); гарантированная доставка (best-effort + анти-энтропия; остаточные риски — §10.9–10.10).

---

## 2. Архитектура

### 2.1 Идентичность

1. Личность — пара ключей $(x, pk)$, Ed25519 (§8). Секрет не публикуется.
2. Регистрации нет: ключ существует, если фигурирует в живой записи. Sybil-устойчивость обеспечивает граф, а не реестр.
3. Один человек может владеть несколькими ключами (§10.2); перенос истории между ключами протоколом не гарантируется (§7.3.3, §10.2).

### 2.2 Сеть без журнала

**Объекты узла:** БД состояния (живые записи + ретроспективный буфер), очередь синхронизации.

1. **Транспорт = социальный граф.** Запись ретранслируется только узлам, с которыми есть активная связь PoSoc. Компонент связности — изолированная вселенная данных: записи человеческого компонента физически не достигают бот-кластеров (двойная изоляция). Свежий ключ без связей не получает трафика; установление связи включает слив состояния.
2. **Обработка записи:** проверка (подписи, каноническая форма, применимость, лимиты §2.6) → применение к БД → постановка в очередь синхронизации → рассылка всем соседям. Ретрансляция — **обязанность** протокола.
3. **Анти-энтропия.** Периодическая сверка состояния с каждым соседом (реконсиляция множеств по диапазонам времени и хешам); расхождения подтягиваются. Утаивание записей соседом обнаруживается сверкой через других соседей; риск единственного моста — §10.9.
4. **Обязательная зона хранения узла:** записи, затрагивающие его собственные связи и группы. Глубина хранения остального — политика узла; полная копия компонента — роль магистрального узла, не обязанность.
5. **Горизонты хранения (GC):** записи пары — до $t_{last} + 2T_{life}$ ($t_{last}$ — позднейшая $t_{sign}$ среди записей пары; обоснование — лемма §3.4); SELF-CONF/VOUCH/DECLARE/отзывы — до конца их эффекта ($t_{sign}+T_{life}$); GENESIS — пока на неё ссылается живая запись. Ретроспектива глубже горизонтов не гарантируется; корректность **текущего** состояния — гарантируется.
6. **Масштабирование:** хранение пропорционально обязательной зоне; полные узлы и лёгкие клиенты различаются только глубиной хранения, не правилами валидации.

### 2.3 Записи (события)

Все записи — канонический CBOR (§2.7); $t_{sign}$ входит в подписываемую полезную нагрузку. Два класса: **объекты согласия** (TTL, автопродление, отзыв) и **действия** (мгновенный эффект).

| Запись | Подписант(ы) | Класс | Эффект |
|---|---|---|---|
| `LINK(u,v)` | обе стороны | объект, $T_{life}$ | экземпляр связи пары (§3) |
| `BREAK(u→v, memo?)` | $u$ | действие | терминирует экземпляр + метка яда (§3.2) |
| `SELF-CONF(P,G)` | $P$ | объект, $T_{life}$ | само-подтверждение членства в L0 $G$ (§4) |
| `VOUCH(Q,P,G)` | $Q$ | объект, $T_{life}$ | поручительство члена $Q$ за $P$ в $G$ (§4) |
| `REVOKE-CONF(Q,P,G)` | $Q$ | действие | отзыв вуша (§4) |
| `L0-LEAVE(P,G)` | $P$ | действие | отзыв само-подтверждения (§4) |
| `L0-GENESIS(initiator, descriptor)` | initiator | действие | адрес L0 (§4.5) |
| `DECLARE(P,H)` | $P$ | объект, $T_{life}$ | голос в C1-кворумах (§5.2) |
| `G-LEAVE(P,H)` | $P$ | действие | отзыв декларации (§5.2) |

Пакетирование: `VOUCH-BATCH(Q, G, [pk…])` — компактная кодировка набора вушей, эквивалентная отдельным записям. `memo` — публичный текст ≤ 256 Б.

Событий слияний, вето, якорей, версий составов не существует.

### 2.4 Время, конфликты, детерминизм

1. **$t_{sign}$** — заявленное автором время, подписано. **$now()$** — локальные часы узла. Состояние вычисляется «на момент» $\tau$.
2. **Будущее.** $t_{sign} > now+\Delta$ ($\Delta=7$ сут.) — запись **отбрасывается навсегда** (защита от предподписанных угонщиком «записей из будущего»). $t_{sign} \in (now,\ now+\Delta]$ — хранится и ретранслируется, к состоянию не применяется, пока часы не догонят.
3. **Прошлое не ограничено.** Поздняя публикация принимается; ретроспективные эффекты ограничены горизонтами объектов (метка не живёт позже базового окна разорванного экземпляра; §3.4).
4. **Разрешение конфликтов.** Для TTL-объектов одного назначения действует запись с **максимальным $t_{sign}$** (обновление замещает). При равных $t_{sign}$ — запись с **меньшим $H(\text{записи})$** (BLAKE3 канонических байтов). Отзыв действует **вперёд** от своего $t_{sign}$: убивает объекты, чья $t_{sign}$ не превосходит его; более поздние обновления — свежее согласие.
5. **Действия (BREAK, отзывы) реконструируются** вместе со всем состоянием: записи упорядочиваются по $(t_{sign}, H)$ и применяется последовательность эффектов (§3.2). Результат не зависит от порядка получения записей узлом.
6. **Живость объекта согласия на $\tau$:** $t_{sign} \le \tau < t_{sign}+T_{life}$ ∧ не отозван записью с $t_{sign} \le \tau$.
7. **Определение состояния.** Состояние = чистая функция (множество принятых записей, $\tau$). Норматив — полная реконструкция; инкрементальные вычисления допустимы при совпадении результата. Узлы с равными множествами записей вычисляют равные состояния при равном $\tau$; расхождения из-за ске часов и задержек доставки — норма, сходимость через §2.2.3.
8. **Правила игнорирования (детерминированные):** неверная подпись; неканоническая форма; $t_{sign} > now+\Delta$; неприменимость (`BREAK` без экземпляра, §3.2; вуш от не-члена группы, §4.3; запись сверх лимитов §2.6). Результат одинаков у всех узлов.

### 2.5 Синхронизация и сходимость

Gossip при появлении записи + анти-энтропия по расписанию. Согласованность — в пределе (при стабилизации множеств записей внутри компонента); в моменте — субъективные виды. Потребитель доверия всегда читает текущее состояние, а не запомненное. Цензура соседа подавляет доставку, но не искажает семантику: задержанная запись применяется со своей $t_{sign}$.

### 2.6 Антиспам (структурный)

1. **Релевантность:** хранятся и ретранслируются только записи, влияющие на состояние (декларации посторонних — от ключей вне живых $R_{str}$ — не хранятся вовсе).
2. **Капы живых объектов на ключ (уровень состояния, детерминированные):** ≤ 16 SELF-CONF; ≤ 32 DECLARE (сверх — применяются 32 с наибольшими $t_{sign}$, тай-брейк по хешу); вуши действительны только от активных членов (§4.3); LINK капом не ограничен (каждая требует подписи контрагента — стоимость встроена).
3. **Rate-limit обновлений:** обновление объекта с $t_{sign}' < t_{sign}+7$ сут. не замещает прежнее.

### 2.7 Канонизация

Канонический CBOR (ключи отсортированы); пары ключей лексикографически; самосвязи запрещены; $\mathrm{sort}$ — порядок байтовых строк; $H$ — BLAKE3 с доменным разделением по типам записей (§8); округления порогов — $\lfloor x + 10^{-9} \rfloor$.

---

## 3. Связи

### 3.1 Экземпляры

1. Связь — отношение пары ключей $(u,v)$; в каждый момент не более одного текущего экземпляра.
2. `LINK(u,v)` с $t_c = t_{sign}$ создаёт экземпляр; экземпляр с большей $t_c$ **замещает** прежний (архив). Окно жизни (§3.3) отсчитывается заново; метки пары усекаются (§3.2.4).
3. Активная связь на $\tau$: экземпляр не терминирован разрывом и не распался.

### 3.2 BREAK и метка яда

**Реконструкция пары:** записи пары упорядочиваются по $(t_{sign}, H)$ и применяются:

1. `LINK(t_c)` — новый текущий экземпляр; активные метки пары усекаются до $t_c$.
2. `BREAK(u→v)`, $t_{sign}$ — если текущий экземпляр $\ell$ существует и $t_c(\ell) \le t_{sign}$:
   - $t_b = \min\big(t_{sign},\ t_c(\ell) + T_{life}\big)$ — **кламп в базовое окно**; живость экземпляра — не предусловие (разрыв формально распавшегося, но не замещённого экземпляра допустим и создаёт метку; $t_{sign} > t_c + T_{life}$ — метка пуста, эффекта нет);
   - экземпляр $\ell$ **терминируется в $t_b$**;
   - создаётся **метка** против $v$: активна на $\big[t_b,\ \min(t_c(\ell)+T_{life},\ t_{\mathrm{relink}})\big)$, где $t_{\mathrm{relink}}$ — $t_c$ следующего `LINK` пары (нет — $\infty$).
   Если текущего экземпляра нет — запись игнорируется.

**Свойства:**

1. **Направленность.** Травится покинутый. Первый по $t_{sign}$ разрыв экземпляра фиксирует направление яда; позднейшие разрывы того же экземпляра игнорируются.
2. **≤ 1 метки на пару** — автоматически (разрыв терминирует экземпляр; новый требует LINK, который усекает метки). Метки **разных** пар стекируются: $N_a$ ≤ числа различных пар $a$.
3. **Перетирание/перемирие.** `LINK(u,v)` усекает метки пары; окна выживающих связей $u$ и $v$ расширяются немедленно; метки третьих лиц не затрагиваются.
4. **Отмывание недоступно.** Аннулирование метки требует `LINK` пары — подписи обеих сторон, включая разорвавшую. Угонщик не стирает метки друзей жертвы.
5. **Цена грифинга.** Цикл `LINK→BREAK→…` требует подписи «жертвы» на каждый `LINK`; метки пары не накапливаются.
6. **Каскад разрывов** против ключа — публичный сигнал компрометации (§7.4).

### 3.3 Распад (Trust Decay)

$$W_\ell(\tau) = \frac{T_{life}}{1+N_u(\tau)+N_v(\tau)},\qquad d(\ell)=\min\{\tau \ge t_c:\ \tau - t_c \ge W_\ell(\tau)\}$$

1. $W$ кусочно-постоянна; $d(\ell)$ вычисляется сканированием моментов начала/конца меток против $u$ и $v$. **Распад не позднее $t_c + T_{life}$.**
2. Активность $\ell$ = $[t_c,\ \min(d(\ell),\ t_{\mathrm{term}}))$, где $t_{\mathrm{term}}$ — момент терминирующего разрыва.
3. **Необратимость:** расширение окон отодвигает распад выжившего, но не оживляет распавшийся; оживление пары — только её новый `LINK`.

### 3.4 Леммы

1. **Горизонт ретроспективы.** Метка не создаёт эффектов позже $t_c(\ell) + 2T_{life}$ (метка живёт $\le t_c+T_{life}$ и влияет на окна связей, созданных не позже чем на $T_{life}$ позже). Следствие — горизонт GC §2.2.5.
2. Все производные величины (распады, активность, $N$) вычисляются из множества записей без истории применения.

*Пример* ($T_{life}=12$ мес., проверен): разрыв $(a,b)$ в $t{=}2$ (экземпляр $t_c{=}0$) → метка $[2,12)$; связь $(b,c)$ ($t_c{=}0$): окно 6 мес., распад $t{=}6$; перемирие `LINK(a,b)` в $t{=}4$ усекает метку → окно $(b,c)$ немедленно 12 мес., распад $t{=}12$ — выжившая связь спасена; $(a,b)$ — свежий экземпляр до $t{=}16$.

---

## 4. Базовые группы (L0)

### 4.1 Принцип

Листовые группы — **L0** — реальные малые группы (соседи, коллеги, одноклассники). Членство — **непрерывный двусторонний предикат**: само-подтверждение участника ∧ поручительства группы ∧ link-опора. Ни ростер, ни событие не могут включить $P$ без подписи $P$; пребывание — при поддержке большинства. Вход требует согласия обеих сторон, выход и отзыв поддержки — односторонни.

### 4.2 Объекты

- **SELF-CONF(P,G)** — «я в G» (TTL, продлевается клиентом $P$).
- **VOUCH(Q→P,G)** — «P — наш» (TTL, продлевается клиентом $Q$; пакетируется).
- **L0-LEAVE(P,G)** — отзыв SELF-CONF: членство $P$ прекращается немедленно, односторонне.
- **REVOKE-CONF(Q,P,G)** — отзыв вуша; **кворум отзывов** роняет поддержку немедленно — это и есть исключение (события EXPEL не существует).

### 4.3 Предикат членства

$P$ — активный член $G$ ⟺ одновременно:

1. SELF-CONF(P,G) жив;
2. живых VOUCH на $P$ от активных членов $G$ ≥ $\lfloor |M_{act}|/2 \rfloor + 1$;
3. **когезия:** активных связей $P$ с активными членами $G$ ≥ $\lfloor |M_{act}|/2 \rfloor + 1$.

**Вычисление — синхронные раунды:** старт с множества кандидатов (живые SELF-CONF с ≥1 живым вушем); на каждом раунде одновременно удаляются все, нарушающие (2) или (3) относительно текущего множества; итерация до стабилизации. Ростер монотонно убывает ⇒ процесс конечен и детерминирован; результат консервативен (нижняя оценка любого последовательного удаления) — осознанно.

**Гашение:** при $|M_{act}| < 4$ группа гаснет (ростер пуст; рёбра от неё отваливаются мгновенным каскадом §5.6).

**Предел 30:** при $|M_{act}| > 30$ (аномалия, возможна через одновременный рост) членами считаются 30 участников с наибольшим числом живых вушей (тай-брейк $pk$).

### 4.4 Динамика: две скорости

- **Явные действия — мгновенны:** отзыв SELF-CONF (выход), кворум отзывов вушей (исключение), потеря когезии (удаление раундом при следующем пересчёте).
- **Угасание — медленно (≤ $T_{life}$):** expire без продления покрывает только мёртвые/исчезнувшие узлы.
- **Вступление/возвращение:** материализация предиката — живая SELF-CONF + свежие вуши (кворум). После угасания вушей — только заново набранный кворум («полный JOIN»); в норме продление тихое, без участия человека.
- Ростер мгновенно входит в $R_{str}$ → пороги → рёбра (§5.6): каскад обрабатывается мгновенным отрывом.

### 4.5 Генезис

`L0-GENESIS(initiator, descriptor, t_sign)` задаёт адрес $ID_G = H(\texttt{"L0"} \Vert \text{descriptor} \Vert pk_{initiator} \Vert t_{sign})$ — без привилегий. Группа жива, когда ≥4 учредителей удовлетворяют предикату §4.3 (их SELF-CONF и взаимные вуши — обычные объекты). Никаких церемоний: генезис — адрес, остальное — непрерывный предикат.

---

## 5. Эмерджентная иерархия: декларации и выводимые привязки

### 5.1 Принцип

Явные акты — связи, членство в L0, декларации. Рёбра, уровни, валидность — выводимое состояние: детерминированная функция живых объектов, перевычисляемая непрерывно. Церемоний слияний нет; согласие всегда позитивно и отзываемо.

### 5.2 Декларации

1. **DECLARE(P,H)** — подписанное $P$ утверждение «поддерживаю принадлежность сообществ, где я состою, к $H$» (синтаксис: голос в кворум C1 каждого $X$ с $P \in R_{str}(X)$). TTL $T_{life}$, тихо автопродлевается (rate-limit §2.6.3). Одна декларация $P$ голосует во всех соответствующих кворумах — это один человек и одно согласие; дизъюнктный подсчёт по уникальным $pk$ исключает двойной счёт людей.
2. **G-LEAVE(P,H)** — мгновенный односторонний отзыв: живость декларации обрезается в $t_{sign}$. Осцилляция LEAVE↔DECLARE самонаказуема: отрыв мгновенный, возврат — через $T_{hold}$.
3. **Адрес группы:** $ID_H = H(\texttt{"GROUP"} \Vert \text{descriptor} \Vert pk_{founder})$; founder — первая (по $t_{sign}$) декларация, техническая роль без привилегий. Имя — не идентичность (§10.1); идентичность сообщества — структура (привязанные дети).
4. **Изолят** — группа без живых детей: $R_{str} = \varnothing$, адрес не хранится сетью (нет живых деклараций), может бутстрапнуться (§5.5). Декларации посторонних не хранятся (§2.6.1).

### 5.3 Ростеры

1. $R_{decl}(H)$ — участники с живыми декларациями в $H$: заявления, **не носители доверия, в порогах не участвуют**.
2. $R_{str}(H) = \bigcup_{C \in \mathrm{children}(H)} R_{str}(C)$ по уникальным $pk$; для L0 — активный ростер §4; изолят — $\varnothing$. Только $R_{str}$ задаёт численности $|H|$ и принадлежность к иерархии.
3. Участник «в иерархии» ⟺ состоит в валидной L0, транзитивно привязанной к доверенному компоненту.
4. Разделение ростеров исключает циркулярность: заявления не зависят от рёбер; рёбра — от связей и структурных численностей.

### 5.4 Пороги и дизъюнктный подсчёт

1. **Дизъюнктный подсчёт:**
$$U_{X \Rightarrow Y} = \big|\{p \in R_{str}(X):\ \exists q \in R_{str}(Y) \setminus R_{str}(X),\ \text{связь } (p,q) \text{ активна}\}\big|$$
Co-membership не засчитывается: корроборация требует живых связей в **внешнюю** часть контрагента.
2. **Пороги сторон** ($avg = (|X|+|Y|)/2$, $\rho = |big|/|small|$, $f = f(avg)$, $\kappa = \kappa(avg)$):
$$f(m) = \begin{cases} 0.5, & m < 30 \\ 0.05 + 0.45 \cdot 1.5^{-\log_{10}(m/30)}, & 30 \le m \le 10^4 \\ 0.05 + 0.1618 \cdot 4^{-\log_{10}(m/10^4)}, & m > 10^4 \end{cases}$$
$$\gamma(\rho) = \min(1,\ f + c(\rho-1)),\ c = 0.25;\qquad \kappa(m) = \min(\lfloor m/2.5 \rfloor,\ 4500)$$
$$t_{big} = \min\big(|big|,\ \max(\lfloor f \cdot |big| \rfloor,\ \kappa) + 1\big)$$
$$t_{small} = \min\big(|small|,\ \max(\lfloor f \cdot |small| \rfloor,\ \lfloor \gamma \cdot |small| \rfloor,\ \kappa) + 1\big)$$
3. **Пара $(X,Y)$ проходит** ⟺ $U_{X \Rightarrow Y} \ge t_X$ **и** $U_{Y \Rightarrow X} \ge t_Y$.
4. **Инварианты:** (i) при непустых внешних частях и полной взаимной внешней связности пара проходит при любых размерах (при большом пересечении ростеров внешние части малы — оговорка существенна); (ii) $t \le |side|$; (iii) равные пары: $t = \min(m, \max(\lfloor f\,m \rfloor, \kappa(m)) + 1)$; (iv) $\rho{=}1$, $m<30$: $t = \lfloor m/2 \rfloor + 1$ — порог пары равен когезии L0; (v) асимметрия — не запрет, а цена: с ростом $\rho$ требование к малой стороне растёт до единогласия.
5. **Лемма (инъекция одного человека).** Валидна атака «угнанный аккаунт + 30 сибил-ключей»: слинкованные с жертвой сибилы дают $U_{X \Rightarrow Y} = 30 \ge t_X$, но $U_{Y \Rightarrow X} = 1 < t_Y$ — двусторонность порогов отбивает инъекцию через один человеческий ключ.

### 5.5 Живое ребро

**Статусы групп:** *изолят* (0 живых детей, $R_{str}=\varnothing$, без уровня); *вершина* ($\ge 2$ живых детей); *свободная группа* (1 живой ребёнок — возникает только распадом вершины; ребро грандфатерится, уровень и приём детей — на общих основаниях).

Ребро $X \to H$ живо на $\tau$ ⟺ выполнены непрерывно:

- **C1 (согласие):** $\ge \lfloor |X|/2 \rfloor + 1$ участников $R_{str}(X)$ имеют живые декларации в $H$;
- **C2 (корроборация, all-pairs):** для **каждого** живого ребёнка $Y \in \mathrm{children}(H) \setminus \{X\}$ пара $(X,Y)$ проходит (§5.4.3);
- **C3 (ацикличность):** не существует пути $H \Rightarrow^* X$ по живым рёбрам.

**Активация** — совместное условие + окна:

- **Присоединение к живой группе** ($\ge 1$ живого ребёнка): C1–C3 выполняются непрерывно $T_{hold}$ → ребро активируется. Проверка пар кандидата со всеми живыми детьми делает активацию безопасной для них: их C2 после активации удовлетворена по построению.
- **Бутстрап пустой группы:** $\ge 2$ кандидатов с C1-кворумами к $H$; все пары кандидатов проходят; совместное непрерывное выполнение $T_{hold}$ (изменение набора кандидатов перезапускает окно) → все рёбра пачки активируются. С этого момента — фриз $T_f$ вершины $H$ (блокируется только роль ребёнка).
- **Фриз:** $T_f$ от генезиса L0 и от активации вершины бутстрапом; блокирует **новые** активации рёбер вверх, не отрывает существующие и не блокирует детей.

Условия C2/C3 при активации оцениваются в детерминированном порядке (равные моменты — по $H$ объекта); активированное ребро цикла создать не может.

### 5.6 Динамика: мгновенный пересчёт

1. **Отрыв — мгновенный.** Провал C1 у ребра **или** провал любой пары $(Y_i, Y_j)$ → оба ребра пары отпадают немедленно; $H$ пересчитывается немедленно.
2. **Понижение.** Живых детей $< 2$: при 1 — вершина становится **свободной группой** (ребро грандфатерится, $L = 1 + L(\text{ребёнка})$); при 0 — изолят. Прочие рёбра (дети и родители всех сторон) сохраняются; каскад — полный мгновенный пересчёт без искусственных задержек.
3. **Уровни.** $L(H) = 1 + \max_{C} L(C)$; изолят — без уровня; уровни — метаданные. Ожидаемая глубина при ширине вершины $\bar k \sim 5\text{–}10$: $D \approx \log_{\bar k}(P/\overline{|L0|}) \approx 7\text{–}10$.
4. **Параллелизм.** Мультиродительство — штатное состояние DAG; каждая привязка независима; дети не блокируются ничем, кроме собственного фриза.
5. **Пересчёт:** непрерывный, по текущим объектам; пороги всегда по **текущим** структурным численностям — порог, выполненный при активации, может стать невыполнимым (штатно; стоимость удержания рекуррентна). Инкрементальность допустима при совпадении с полной реконструкцией.

### 5.7 Возражения и выход

1. **Лёгкое** — `G-LEAVE`: кворум C1 падает, ребро отрывается мгновенно; связи не страдают.
2. **Тяжёлое** — `BREAK` (§3.2).
3. **Выход группы из родителя** — массовый `G-LEAVE` членов $X$; отдельного события нет.

### 5.8 Механизм изоляции ботов

Декларации permissionless, но: кворумы L0 контролируются людьми; изолят имеет пустой $R_{str}$ и не может корроборировать; дизъюнктный подсчёт исключает co-membership; каждая пара, через которую бот-поддерево соединяется с человеческой иерархией, требует живых человеческих связей, истекающих за $\le T_{life}$. Одиночные рёбра (свободные группы) не открывают обхода: соединяющее ребро всё равно встречает сестёр родителя (следствие анализа — свойство самоисцеления: оторванное от людей поддерево теряет корроборацию не позже чем за $T_{life}$).

### 5.9 Мотивация связей

Алгоритм не регламентирует, кого подтверждать; предполагается, что реальных связей у людей достаточно, чтобы граф связался естественным образом.

---

## 6. Проверка и потребление доверия

1. **Доказательство членства:** предъявляется множество подписанных объектов (L0 заявителя: SELF-CONF + вуши + связи; рёбра пути к корню) — верификатор проверяет подписи локально и запрашивает у нескольких соседей подтверждение полноты (spot-check на скрытые отзывы). Декларации в доказательстве не участвуют.
2. **Субъективная оценка:** доверенное множество — транзитивное замыкание родителей L0 верификатора (+ его политика: размер, возраст, аномалии §7.4). Глобального флага «человек» нет.
3. **Практическая картина:** люди связываются в гигантский компонент; бот-кластеры без человеческих связей остаются изолированными компонентами — структурно и транспортно (§2.2.1).
4. **Непересечение множеств** — условное следствие двусторонних порогов (лемма §5.4.5).

---

## 7. Защитные механизмы

### 7.1 Гистерезис и фриз

$T_{hold}$ — окно непрерывного выполнения условий **перед активацией** (уведомления, возражения, дорост связей). Отрыв мгновенен — асимметрия осознана: включение медленное, отторжение немедленное; флаппинг подавляется ценой повторного включения. $T_f$ — задержка роли ребёнка для свежих вершин (анти-накачка и анти-ротация топологии).

### 7.2 Распад и яд

Окна $T_{life}/(1+N)$, линейное стекирование по разным парам, конечные метки, перетирание перемирием; неподдерживаемые связи исключаются из порогов; потеря когезии исключает из ростера (§4.3). Яд конечен; перемирие — мгновенный путь аннулирования меток пары.

### 7.3 Угон, потеря ключа, боты

1. **Угон.** Друзья публикуют `BREAK` и `REVOKE-CONF` (кворум). Разрывы: $N$ растёт → окна сжимаются в $1+N$ раз ($T_{life}{=}12$ мес., $N{=}6$ — изоляция за ≈1.7 мес.); обновления требуют подписей друзей; их метки угонщик не аннулирует. Угонщик может отозвать декларации жертвы — рёбра отвалятся мгновенно; вред обратим новыми декларациями.
2. **Честное восстановление.** Переверберация лично → друзья подписывают `LINK` (метки аннулируются, экземпляры чисты) → декларации возобновляются. Без «штрафа переживания».
3. **Потеря ключа (ротация):** без специального события — рецепт: новый ключ вступает в L0 (SELF-CONF + кворум вушей) → старый выводится (`L0-LEAVE` при контроле или кворум отзывов при утере) → связи переподписываются контрагентами (`LINK` — перетирание чистит метки пар). Метки не переносятся (привязаны к ключу); вору кворум не собрать; ботам ротация бесполезна (новый ключ входит в иерархию только через живые человеческие связи). Непрерывность «тот же человек» протоколом не гарантируется — признанная негарантия (§10.2).
4. **Боты.** Не проходят порогов без человеческих связей (§5.8); множества людей и ботов не пересекаются в пределах допущений §1.3.

### 7.4 Обнаружение аномалий (advisory)

1. Churn: циклы `LINK/BREAK`, осцилляции одной пары.
2. Каскадные разрывы против одного pk; резкое сжатие окон.
3. Массовые `LINK` сразу после каскада разрывов («прощение-фишинг»).
4. Декларационные паттерны: синхронные DECLARE/LEAVE; спам адресов; набеги на сообщества; namespace-спуфинг дескрипторов.
5. Звёздные топологии; плотность между компонентами.
6. Распределения степеней/плотности против размера (люди — small-world; фабрики — регулярность).
7. Синхронность операций; синхронные пакеты вушей/отзывов.
8. Динамика L0: синхронные вступления/отзывы, текучесть ростеров, состояния >30.
9. Накачка уровней, аномальные глубины, вершины без прироста $R_{str}$, цепочки одиночных детей.
10. DAG-специфика: штормы параллельных привязок, близнецы-родители.
11. Паттерны memo-полей.

---

## 8. Криптография и объёмы

1. **Подписи:** Ed25519 (pk 32 Б, подпись 64 Б); интерфейс допускает постквантовые схемы.
2. **Хеши:** BLAKE3 с доменным разделением по типам записей (`LINK`, `BREAK`, `SELF-CONF`, `VOUCH`, `REVOKE-CONF`, `L0-LEAVE`, `L0-GENESIS`, `DECLARE`, `G-LEAVE`, hash-tiebreak).
3. **Канал уведомлений (опционально):** $s_{uv} = \mathrm{HKDF}(\mathrm{X25519}(x_u, pk_v))$ — для координации продлений и уведомлений о гистерезисных окнах; не источник истины.
4. **Канонизация:** детерминированный CBOR.
5. **Объёмы записей:** `LINK` ≈ 200 Б; `BREAK`/`DECLARE`/`G-LEAVE`/`SELF-CONF` ≈ 150 Б; `VOUCH-BATCH(30)` ≈ 1 КБ.
6. **Нагрузка:** на человека в год ≈ 60 продлений связей + 2 SELF-CONF + 2 пакета вушей + ~$2D$ ≈ 16–20 деклараций ≈ **80–100 тихих записей/год** (~10 КБ). При $P = 10^8$: ≈ $10^{10}$ записей/год ≈ 300/с по сети; суммарное хранение ≈ 1.5–2 ТБ/год; обязательная зона узла — мегабайты–гигабайты.

---

## 9. Параметры (значения по умолчанию)

| Параметр | Значение |
|---|---|
| $T_{life}$ (TTL всех объектов согласия; базовое окно связи) | 12 мес. |
| Автопродление объектов | при ≥50% истечения; rate-limit обновлений 1/7 сут. |
| $\Delta$ (горизонт будущего) | 7 сут.; сверх — отбрасывается |
| $T_{hold}$ (активация рёбер и бутстрапа) | 7 сут. |
| $T_f$ (фриз роли ребёнка) | 14 сут.; триггеры: генезис L0, активация вершины бутстрапом |
| Отрыв рёбер | мгновенный (провал C1 или любой пары); пересчёт полный |
| Тай-брейк равных $t_{sign}$ | меньший $H(\text{записи})$ |
| L0 | $4 \le |M_{act}| \le 30$; когезия и вуш-кворум $\lfloor |M|/2 \rfloor + 1$; гашение <4; синхронные раунды |
| C1 | $\lfloor |X|/2 \rfloor + 1$ живых деклараций от $R_{str}(X)$ |
| C2 | all-pairs со всеми живыми сёстрами |
| C3 | ацикличность (путь $H \Rightarrow^* X$ не существует) |
| Бутстрап | $\ge 2$ кандидатов с C1; все пары проходят; совместное $T_{hold}$ |
| Пороги пар (§5.4) | $f$: $f(m<30)=0.5$; $b_1=1.5$, $m^*=10^4$, $b_2=4$, $f_\infty=0.05$ |
| Скорость единогласия | $c=0.25$; $\gamma=1$ при $\rho \ge 1+4(1-f)$ |
| Пол заговора $\kappa$ | $\min(\lfloor m/2.5 \rfloor,\ 4500)$ |
| Подсчёт корроборации | дизъюнктный, во внешнюю часть |
| Яд | $T_{life}/(1+N)$; метка $\le$ базового окна экземпляра; ≤1 на пару |
| Перетирание | `LINK(u,v)` усекает метки пары |
| Капы на ключ | 16 SELF-CONF; 32 DECLARE; вуши — только от активных членов |
| Горизонты хранения | пара: $t_{last}+2T_{life}$; объекты согласия: до конца TTL |
| Подписи / хеши | Ed25519 / BLAKE3 |
| Ожидаемая глубина $D$ | 7–10 (при ширине вершины 5–10) |

**Справочные пороги равных пар** ($t = \min(m, \max(\lfloor f(m)\,m \rfloor, \kappa(m)) + 1)$):

| $m$ | $f(m)$ | $t$ | доля |
|---|---|---|---|
| 300 | 0.350 | 121 | 40% |
| $10^3$ | 0.293 | 401 | 40% |
| $3\times10^3$ | 0.250 | 1201 | 40% |
| $10^4$ | 0.212 | 4001 | 40% |
| $3\times10^4$ | 0.134 | 4501 | 15% |
| $10^5$ | 0.090 | 9046 | 9% |
| $10^6$ | 0.060 | 60113 | 6% |
| $10^8$ | 0.051 | 5063204 | 5% |

**Примеры:**

| Конфигурация | Требования |
|---|---|
| Бутстрап L1: две L0(30) | кворумы 16/30 и 16/30; пара 16 ↔ 16 |
| Бутстрап L1: три L0(30) | попарно по 16 |
| Присоединение L0(30) к H = {L0(30), L0(30)} | кворум 16/30; пары 16↔16 с каждым ребёнком |
| L0(30) ↔ L0(60) | 22 из 30 (73%) и 29 из 60 (48%) |
| L0(4) ↔ L0(8) | 4 из 4 и 5 из 8 |
| L0(4) ↔ L0(30) | 4 из 4 (единогласно) и 16 из 30 |
| Co-membership (две L0(30), 10 общих, связи только через общих) | $U = 0 < 16$: не проходит |

---

## 10. Ограничения и открытые вопросы

1. **Namespace:** имперсонация имён нерешаема; идентичность — структура.
2. **Многоличность:** принципиально не детектируется; смягчения — стоимость связей, эвристики.
3. **Публичность согласия:** видны составы и декларации; приватизация — §11.
4. **Мажоритарная жёсткость:** индивидуум не привяжется без большинства своей L0; возможен режим индивидуальных деклараций без структурной силы.
5. **Сговор выше порогов:** $\kappa$ ограничивает пол заговора; выше — вне гарантий.
6. **Спам:** структурно ограничен (релевантность, капы, rate-limits); остаточное — объём буферов.
7. **Грифинг ядом:** каскады разрывов сжимают окна; ограничители — взаимность, конечность, перемирие; остаточный риск признан.
8. **Угон и клиентские политики:** явные подтверждения всех операций; предупреждения о контрагентах с историей разрывов.
9. **Цензура соседа:** утаивание записей; анти-энтропия и мультиродительство смягчают; единственный мост — риск.
10. **Доступность данных:** прувер может скрывать отзывы; spot-checks у нескольких соседей смягчают; скоординированное сокрытие — остаточный риск.
11. **Дрейф порогов при росте:** рост брата повышает парный порог при статичных связях — отрыв и дорогой возврат; строгость осознана, живучесть — мультиродительством.
12. **Сложность вывода рёбер:** all-pairs — $O(\sum_H k_H^2)$ за пересчёт; обязательны индексы и инкрементальность.
13. **Консервативность раундов когезии:** синхронное удаление — нижняя оценка; осознанно (безопасность важнее полноты).
14. **Расхождения часов:** влияют на мгновенные виды, не на сходимость; $\Delta$ покрывает скью.
15. **Управление параметрами:** изменение = форк; governance не специфицирован.

---

## 11. Приватность через ZKP (дорожная карта, вне базовой версии)

Публичный граф и декларации — тяжёлый компромисс. Эскиз: обязательства Педерсена, деревья Меркла ростеров, публичные корни/уровни/численности, нуллификаторы против двойного счёта, рекурсивная агрегация. Специфические для v0.9 блокеры: доказуемое воспроизведение LWW по $t_{sign}$ и реконструкции BREAK (кламп, перетирание) в схемах; TTL-объекты и обновления обязательств; поиск кандидатов без публичного графа; масштаб; постквантовость.

---

## 12. Эталонные реализации

Спецификация нормативна; реализации v0.9 — **отдельный артефакт**. Обязательные компоненты: (1) реконструкция пары — §3; (2) синхронные раунды ростера L0 — §4.3; (3) вывод рёбер — all-pairs C2, ацикличность C3, окна $T_{hold}$, бутстрап — §5.5–5.6.

---

## 13. ГЛОССАРИЙ

**Время и состояние**

- **$t_{sign}$** — подписанное автором время записи; единственный источник времени в протоколе (§2.4).
- **$now()$** — субъективные часы узла; состояние всегда вычисляется «на момент».
- **Правило будущего** — $t_{sign} > now+\Delta$: отбрасывается; в коридоре $\Delta$: откладывается до догонa часов (§2.4.2).
- **LWW** — среди TTL-объектов одного назначения действует запись с максимальной $t_{sign}$ (§2.4.4).
- **Тай-брейк** — равные $t_{sign}$: применяется запись с меньшим BLAKE3-хешем (§2.4.4).
- **Объект согласия** — TTL-запись с автопродлением и мгновенным отзывом; четыре типа: LINK, SELF-CONF, VOUCH, DECLARE (§2.3).
- **$T_{life}$ / TTL** — срок жизни объекта согласия; живость $[t_{sign},\ t_{sign}+T_{life})$ минус отзыв (§2.4.6).
- **Автопродление** — тихая перевыпись клиентом при ≥50% истечения; не чаще 1/7 сут. (§2.6.3).
- **Отзыв** — событие-действие, обрезающее жизнь объекта с момента своей $t_{sign}$ (§2.4.4).
- **Реконструкция** — нормативное вычисление состояния: записи сортируются по $(t_{sign}, H)$ и применяются последовательно; порядок доставки семантики не имеет (§2.4.5).

**Связи**

- **Экземпляр связи** — LINK-объект пары; новый замещает прежний (§3.1).
- **BREAK** — односторонний разрыв: терминирует экземпляр, создаёт метку яда (§3.2).
- **Кламп** — $t_b = \min(t_{sign},\ t_c+T_{life})$: разрыв зажат в базовое окно разрываемого экземпляра (§3.2.2).
- **Метка яда** — активна на $[t_b,\ \min(t_c+T_{life},\ t_{relink}))$; травит покинутого; ≤1 на пару; стекируется по разным парам (§3.2).
- **$N_a$** — число активных меток против $a$ (§3.2).
- **Окно жизни** — $W = T_{life}/(1+N_u+N_v)$ (§3.3).
- **Распад (decay)** — $d(\ell) = \min\{\tau: \tau - t_c \ge W(\tau)\}$; необратим; оживление — только новый LINK пары (§3.3).
- **Перетирание / перемирие** — LINK пары усекает её метки; расширяет окна выживающих связей (§3.2.3).
- **Первый разрыв** — направление яда фиксирует первый по $t_{sign}$ разрыв экземпляра (§3.2, св. 1).

**Базовые группы**

- **L0** — листовая группа, $4 \le |M_{act}| \le 30$ (§4).
- **SELF-CONF** — само-подтверждение членства, TTL-объект (§4.2).
- **Вуш (VOUCH)** — поручительство члена за кандидата; кворум $\lfloor |M|/2 \rfloor + 1$ (§4.3).
- **REVOKE-CONF** — отзыв вуша; кворум отзывов = исключение, мгновенно (§4.2).
- **L0-LEAVE** — односторонний выход (отзыв SELF-CONF) (§4.2).
- **Когезия** — $\ge \lfloor |M_{act}|/2 \rfloor + 1$ активных связей с активными членами (§4.3.3).
- **Синхронные раунды** — детерминированная фикс-точка членства: одновременное удаление нарушителей, итерация до стабилизации (§4.3).
- **Гашение группы** — $|M_{act}| < 4$: ростер пуст, рёбра отваливаются каскадом (§4.3).

**Иерархия**

- **Адрес группы** — $ID_H$ из дескриптора и pk учредителя; без привилегий; идентичность сообщества — структура (§5.2.3).
- **DECLARE** — голос «поддерживаю принадлежность к $H$» в C1-кворумах всех групп, где декларант в $R_{str}$; TTL 12 мес. (§5.2.1).
- **G-LEAVE** — мгновенный отзыв декларации; «лёгкое возражение» (§5.2.2, §5.7).
- **Изолят** — 0 живых детей; $R_{str}=\varnothing$; не хранится сетью; не может корроборировать (§5.2.4, §5.8).
- **Вершина** — группа с ≥2 живыми детьми (§5.5).
- **Свободная группа** — распавшаяся вершина с одним ребёнком; ребро грандфатерится; приём детей и привязка вверх — на общих основаниях (§5.5, §5.6.2).
- **Ребро $X \to H$** — X ребёнок, H родитель; живо при непрерывных C1–C3 (§5.5).
- **C1** — кворум деклараций: $\lfloor |X|/2 \rfloor + 1$ живых от $R_{str}(X)$ (§5.5).
- **C2 (all-pairs)** — корроборация: пары со **всеми** живыми сёстрами (§5.5).
- **C3** — ацикличность: путь $H \Rightarrow^* X$ не существует (§5.5).
- **$T_{hold}$** — окно непрерывного выполнения условий перед активацией ребра/бутстрапа (§5.5).
- **$T_f$ / фриз** — блокировка роли ребёнка после генезиса L0 и активации вершины; не отрывает существующие рёбра (§5.5).
- **Бутстрап** — совместная активация ≥2 рёбер к пустой группе; все пары кандидатов проходят; окно $T_{hold}$ (§5.5).
- **Отрыв** — мгновенный: провал C1 или любой пары (оба ребра пары) (§5.6.1).
- **$R_{str}$ / $R_{decl}$** — структурный ростер (союз живых детей; задаёт численности) / ростер-заявления (не носитель доверия) (§5.3).
- **Уровень $L$** — $1+\max$ по живым детям; метаданные, не критерий доверия (§5.6.3).
- **Дизъюнктный подсчёт $U$** — корроборация только во внешнюю часть контрагента (§5.4.1).
- **Порог стороны $t$; кривая $f$; $\gamma$; пол заговора $\kappa$** — пороговая арифметика пар (§5.4.2).
- **Мультиродительство** — группа одновременно ребёнок любого числа родителей; штатное состояние DAG (§5.6.4).

**Сеть и синхронизация**

- **Сосед** — узел с активной связью; транспорт совпадает с графом доверия (§2.2.1).
- **Очередь синхронизации** — исходящий буфер ретрансляции (§2.2).
- **Анти-энтропия** — периодическая сверка множеств записей с соседями; подавляет цензуру и потери (§2.2.3).
- **Обязательная зона хранения** — свои связи и группы; глубже — политика узла (§2.2.4).
- **Горизонт хранения** — GC-правила: пара до $t_{last}+2T_{life}$, объекты согласия до конца TTL (§2.2.5).
- **Капы / rate-limits** — детерминированные лимиты живых объектов и обновлений на ключ (§2.6).
- **Spot-check** — запрос полноты данных у нескольких соседей при проверке доказательства (§6.1).

**Доверие**

- **Доверенное множество** — транзитивное замыкание родителей L0 верификатора (+ его политика) (§6.2).
- **Стоимость включения** — $\approx t$ взаимных живых связей, рекуррентно $\le T_{life}$ (§1.3).
- **Локализация угона** — сжатие окон в $1+N$; метки друзей нестерираемы (§7.3.1).

---
